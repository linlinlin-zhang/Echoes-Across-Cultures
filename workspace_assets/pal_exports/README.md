# PAL Exports

## 中文说明

这个目录用于存放正在被当前 V4 主线实际使用的 PAL 人工导出文件。

### 当前文件

- `pal_v4_main_annotation_human_export_200pairs.csv`：当前迁移到 V4 主线的核心真人 PAL 导出文件
- `pal_v4_main_annotation_progress_snapshot.json`：同一轮标注流程的进度快照

这些文件属于当前在用的人工输入，不是自动生成的 benchmark 输出。

## English Notes

This directory stores hand-managed PAL exports that are actively used to align human judgments with the V4 mainline.

### Current files

- `pal_v4_main_annotation_human_export_200pairs.csv`: main human PAL export used by migrated V4 PAL configs
- `pal_v4_main_annotation_progress_snapshot.json`: progress snapshot from the same annotation workflow

These are active manual inputs, not generated benchmark outputs.
