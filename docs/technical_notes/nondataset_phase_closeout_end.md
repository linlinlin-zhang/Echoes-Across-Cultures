# 非数据集阶段收口与数据集阶段入口

日期：2026-02-28  
负责人：Codex

## 本阶段收口状态
已完成并入库准备：
1. `Minority Exposure@K` 指标全链路接入。
2. 阶段2：`TC/HSIC` 敏感性网格脚本与稳定性证据输出。
3. 阶段3：`VAE / beta-VAE / FactorVAE` 基线对比脚本与必要性检查。
4. 兼容性修复：
- `shared_encoder=True` 下对比损失调用 bug 修复。
- 推荐结果比较脚本支持“指标缺失容错”。

## 数据集阶段建议入口（下一阶段）
1. 数据源层：
- 公开数据集清单冻结（多文化目标覆盖 + 版权可用性）
- 每数据源写明 `culture/label/affect_label/audio_path` 映射策略

2. 数据质量层：
- 元数据去重与冲突合并
- 分布报告（文化/标签/时长/采样率/缺失率）
- 训练前门禁（split 泄漏检测 + 长尾覆盖）

3. 训练层：
- 先固定 backbone 特征抽取，完成可复现主表
- 再评估是否进入 backbone 轻量微调（LoRA/局部解冻）

4. 论文证据链层：
- 统一主表字段与统计检验方案
- 保留每次 run 的配置与随机种子映射

