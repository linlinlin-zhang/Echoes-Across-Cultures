# Web Prototype

## 中文说明

这个目录包含当前仍在保留的 prototype 前端资源。

### 当前状态

- 在旧 `web/` 前端被移除后，这里是现役 prototype/frontend 目录
- 它适合做 demo、交互展示和未来的数据采集入口
- 但它不是当前论文系统主线的第一优先级

### 与主线的关系

当前论文应优先由 `dcas/`、`configs/`、`storage/`、`reports/` 和 `paper/` 驱动。等论文主线稳定后，再回头继续打磨 prototype 更合适。

## English Notes

This directory contains the current prototype frontend assets.

### Status

- This is the active prototype/frontend location after the older `web/` frontend was removed
- It is useful for demo, interaction, and future data-collection flows
- It is not the primary paper-system mainline

### Mainline relationship

The paper should be driven first by `dcas/`, `configs/`, `storage/`, `reports/`, and `paper/`.
The prototype can be revisited after the paper-system mainline is stable.
