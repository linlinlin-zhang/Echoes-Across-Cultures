# Lightweight cloud deployment dependencies.
# This serves the static web app, anonymous sessions/favorites, catalog browsing,
# audio streaming, and worker proxy endpoints. Install the full requirements.txt
# only on the local research worker that runs CultureMERT/DCAS inference.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
numpy>=1.26
mutagen>=1.47
